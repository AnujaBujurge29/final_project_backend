{
  "name": "backend",
  "version": "1.0.0",
  "description": "New(GET) = Give me a form so I can create a thing\r Create(POST)= Create a thing using this form a filled out\r Show(GET) = show me a thing with this id\r Edit(GET)= Give me a prefilled form so I can change something on the thing\r Update(PUT)= Update the thing with this changed form\r Delete(DELETE)= Get rid of the thing with this id",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^16.0.3",
    "express": "^4.18.2",
    "jsx-view-engine": "^1.0.0",
    "mongoose": "^6.11.1",
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  }
}
